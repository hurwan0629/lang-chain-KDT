# 메시지 큐 더 파먹기
일단 스크립트에서 `amqp.connect()`를 이용해서 tcp 연결을 수립해놓고

`connection.createChannel()`을 이용해서 채널? (이게 무슨 역할을 하는건지 잘 모르겠다. 하나의 connection에서 2개의 채널이 만들어지면 무슨 의미이고 그 채널에 다른 연결이 접속한다던가 뭔가 큐를 공유한다던가 할 수 있는건가? 뭐지)

그리고 그 채널에서 Exchange를 하나 만들면 