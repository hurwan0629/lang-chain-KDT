# 이중 연결 리스트
> 과제...! 해야한다!

이번에는 연결 리스트라는 자료구조를 공부 및 구현하는 과제를 받았습니다.

하지만 이미 연결/이중 연결리스트에 대해서 알고있었기 때문에 개인적으로 하고있는 로우레벨 언어를 통한 세세한 구현을 해보았습니다.

## 개념
일단 (이중)연결 리스트를 설명하고 가자면, 노드간의 연결을 통해 만들어지는 리스트를 말합니다. 이는 이전에 다뤘던 배열(`Array`)와 다르게 연속되지 않은 메모리를 가지게 됩니다. 이 때문에 인덱스를 알아도 접근에 걸리는 시간은 `O(n)`만큼 걸리며, 일반적으로 공간을 더 많이 차지합니다. **하지만** 이중 연결리스트(연결리스트)는 삽입/삭제에 대해 위치(노드 주소)를 알면 `O(1)`이라는 속도로 삽입/삭제가 가능하기 때문에 삽입/삭제가 많은 환경에서는 해당 자료구조를 사용하는 것이 좋습니다.

## 코드